name: Build sdr-ingestion-rest image

on:
  workflow_dispatch:

jobs:
  build-docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: echo
        run: |
          curl -X POST https://api.github.com/repos/kopachevsky/deploy/dispatches \
          -H 'Accept: application/vnd.github.everest-preview+json' \
          -u ${{ secrets.ACCESS_TOKEN }} \
          --data '{"event_type": "123", "client_payload": { "service": "serviceName" }}'